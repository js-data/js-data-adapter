general:
  artifacts:
    - dist
  branches:
    ignore:
      - gh-pages
machine:
  node:
    version: 13
dependencies:
  pre:
    - npm i -g npm
    - npm i -g codecov nyc
test:
  post:
    - nyc report --reporter=lcov | codecov
